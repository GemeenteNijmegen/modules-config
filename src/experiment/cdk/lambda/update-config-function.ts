// ~~ Generated by projen. To modify, edit .projenrc.ts and run "npx projen".
import * as path from 'path';
import * as lambda from 'aws-cdk-lib/aws-lambda';
import { Construct } from 'constructs';

/**
 * Props for UpdateConfigFunction
 */
export interface UpdateConfigFunctionProps extends lambda.FunctionOptions {
}

/**
 * An AWS Lambda function which executes src/experiment/lambda/update-config.
 */
export class UpdateConfigFunction extends lambda.Function {
  constructor(scope: Construct, id: string, props?: UpdateConfigFunctionProps) {
    super(scope, id, {
      description: 'src/experiment/lambda/update-config.lambda.ts',
      ...props,
      runtime: new lambda.Runtime('nodejs24.x', lambda.RuntimeFamily.NODEJS),
      handler: 'index.handler',
      code: lambda.Code.fromAsset(path.join(__dirname, '../../../assets/experiment/lambda/update-config.lambda')),
    });
    this.addEnvironment('AWS_NODEJS_CONNECTION_REUSE_ENABLED', '1', { removeInEdge: true });
  }
}